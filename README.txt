1) Abrir la terminal y dirigirse al directorio de la carpeta TPE usando el comando cd.
2) Usar "make" para crear el ejecutable.
3) Al finalizar la compilacion ejecutar usando ./Run

Los archivos fuente deben el mismo directorio que el resto de los archivos del programa.

